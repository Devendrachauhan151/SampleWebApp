pipeline {
    agent any

    stages {
        stage('git checkout') {
            steps {
                git 'https://github.com/Devendrachauhan151/SampleWebApp.git'
            }
        }
    }
}
